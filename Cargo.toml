[workspace]
resolver = "2"
members = [
    "aminos",
    "cli",
    "color",
    "config",
    "env",
    "ls",
    "net",
    "sys",
]

[profile.release]
opt-level = "z"
lto = true
strip = true
codegen-units = 1
