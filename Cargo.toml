[workspace]
resolver = "2"
members = [
    "aminos",
    "color",
    "config",
    "ls",
    "net",
    "path-rules",
    "pp",
    "ss",
    "sys",
]

[profile.release]
opt-level = "z"
lto = true
strip = true
codegen-units = 1
