[workspace]
resolver = "2"
members = [
    "aminos",
    "color",
    "config",
    "env",
    "ls",
    "net",
    "sys",
]

[profile.release]
opt-level = "z"
lto = true
strip = true
codegen-units = 1
